import { Contact, Conversation } from '@apirtc/apirtc';
export default function useConversationModeration(conversation: Conversation | undefined, onEjected?: (contact: Contact) => void, onEjectedSelf?: () => void): {
    candidates: Set<Contact>;
};
