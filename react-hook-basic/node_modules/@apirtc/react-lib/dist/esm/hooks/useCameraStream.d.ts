import { CreateStreamOptions, Session, Stream } from '@apirtc/apirtc';
export default function useCameraStream(session: Session | undefined, options?: CreateStreamOptions): {
    stream: Stream | undefined;
};
